<h1>Listing leave_configs</h1>

<table>
  <tr>
    <th>Emptype</th>
    <th>Sl</th>
    <th>Cl</th>
    <th>El</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @leave_configs.each do |leave_config| %>
  <tr>
    <td><%= leave_config.emptype %></td>
    <td><%= leave_config.sl %></td>
    <td><%= leave_config.cl %></td>
    <td><%= leave_config.el %></td>
    <td><%= link_to 'Show', leave_config %></td>
    <td><%= link_to 'Edit', edit_leave_config_path(leave_config) %></td>
    <td><%= link_to 'Destroy', leave_config, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Leave config', new_leave_config_path %>
